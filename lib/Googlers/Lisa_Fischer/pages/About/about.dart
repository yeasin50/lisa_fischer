import 'package:flutter/material.dart';

class LSAboutPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return CustomScrollView(
         
      slivers: [
        
      ],
    );
  }
}
